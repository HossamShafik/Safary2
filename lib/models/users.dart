class Travelers{

  String username,password,firstName,lastName,address,phone,gender,image,id;
  Travelers({this.id,this.username, this.password, this.firstName, this.lastName,
      this.address, this.phone, this.gender, this.image});
}